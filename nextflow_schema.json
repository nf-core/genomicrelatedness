{
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "$id": "https://raw.githubusercontent.com/nf-core/genomicrelatedness/dev/nextflow_schema.json",
    "title": "nf-core/genomicrelatedness pipeline parameters",
    "description": "A workflow for lcWGS genotype likelihood calling and relatedness estimation",
    "type": "object",
    "$defs": {
        "input_output_options": {
            "title": "Input/output options",
            "type": "object",
            "fa_icon": "fas fa-terminal",
            "description": "Define where the pipeline should find input data and save output data.",
            "required": ["input", "outdir"],
            "properties": {
                "input": {
                    "type": "string",
                    "format": "file-path",
                    "exists": true,
                    "schema": "assets/schema_input.json",
                    "mimetype": "text/csv",
                    "pattern": "^\\S+\\.csv$",
                    "description": "Path to comma-separated file containing information about the samples in the experiment.",
                    "help_text": "You will need to create a design file with information about the samples in your experiment before running the pipeline. Use this parameter to specify its location. It has to be a comma-separated file with 3 columns, and a header row. See [usage docs](https://nf-co.re/genomicrelatedness/usage#samplesheet-input).",
                    "fa_icon": "fas fa-file-csv"
                },
                "outdir": {
                    "type": "string",
                    "format": "directory-path",
                    "description": "The output directory where the results will be saved. You have to use absolute paths to storage on Cloud infrastructure.",
                    "fa_icon": "fas fa-folder-open"
                },
                "email": {
                    "type": "string",
                    "description": "Email address for completion summary.",
                    "fa_icon": "fas fa-envelope",
                    "help_text": "Set this parameter to your e-mail address to get a summary e-mail with details of the run sent to you when the workflow exits. If set in your user config file (`~/.nextflow/config`) then you don't need to specify this on the command line for every run.",
                    "pattern": "^([a-zA-Z0-9_\\-\\.]+)@([a-zA-Z0-9_\\-\\.]+)\\.([a-zA-Z]{2,5})$"
                },
                "multiqc_title": {
                    "type": "string",
                    "description": "MultiQC report title. Printed as page header, used for filename if not otherwise specified.",
                    "fa_icon": "fas fa-file-signature"
                }
            }
        },
        "reference_genome_options": {
            "title": "Reference genome options",
            "type": "object",
            "fa_icon": "fas fa-dna",
            "description": "Reference genome related files and options required for the workflow.",
            "properties": {
                "fasta": {
                    "type": "string",
                    "format": "file-path",
                    "exists": true,
                    "mimetype": "text/plain",
                    "pattern": "^\\S+\\.fn?a(sta)?(\\.gz)?$",
                    "description": "Path to FASTA reference genome file.",
                    "help_text": "Provide the absolute or relative path to the FASTA file used as reference genome. The pipeline will automatically create BWA and FAI indices if they are missing.",
                    "fa_icon": "fas fa-file-code"
                },
                "fasta_fai": {
                    "type": "string",
                    "format": "file-path",
                    "exists": true,
                    "mimetype": "text/plain",
                    "pattern": "^\\S+\\.fn?a(sta)?(\\.gz)?\\.fai$",
                    "description": "Path to FASTA index (.fai) file for the reference genome.",
                    "help_text": "Optional. Provide an existing FAI index file. If omitted, the pipeline will generate one automatically using samtools faidx.",
                    "fa_icon": "fas fa-file-code"
                },
                "dict": {
                    "type": "string",
                    "format": "file-path",
                    "exists": true,
                    "mimetype": "text/plain",
                    "pattern": "^\\S+\\.dict$",
                    "description": "Path to sequence dictionary (.dict) for the reference genome.",
                    "help_text": "Optional. Provide an existing GATK-style sequence dictionary. If omitted, the pipeline will create it using GATK CreateSequenceDictionary.",
                    "fa_icon": "fas fa-file-alt"
                },
                "bwamem2_index": {
                    "type": "string",
                    "format": "directory-path",
                    "exists": true,
                    "description": "Path to directory containing the BWA-MEM2 index for the reference genome.",
                    "help_text": "Optional. Provide a directory with pre-built BWA-MEM2 index files (e.g., .0123, .amb, .ann, .bwt.2bit.64, .pac). If omitted, the pipeline will generate the index automatically.",
                    "fa_icon": "fas fa-folder-open"
                },
                "intervals": {
                    "type": "string",
                    "format": "directory-path",
                    "exists": true,
                    "description": "Path to directory containing the intervals in bed format.",
                    "help_text": "Optional. Provide a directory with pre-built interval files (e.g., interval_001.bed, interval_002.bed, …). If omitted, the pipeline will generate intervals automatically.",
                    "fa_icon": "fas fa-folder-open"
                },
                "target_number_of_interval_files": {
                    "type": "integer",
                    "minimum": 1,
                    "maximum": 1000,
                    "description": "Number of interval files to split the reference genome into for parallel processing.",
                    "help_text": "Number of interval files to split the reference genome into for parallel processing during alignment and variant calling. Higher numbers can improve performance on large genomes but may increase overhead.",
                    "fa_icon": "fas fa-hashtag"
                },
                "max_number_of_intervals_per_file": {
                    "type": "integer",
                    "minimum": 1,
                    "maximum": 1000,
                    "description": "Maximum number of intervals per interval file for parallel processing.",
                    "help_text": "Maximum number of intervals per interval file for parallel processing during alignment and variant calling. Lower numbers can reduce overhead but may decrease parallelism.",
                    "fa_icon": "fas fa-hashtag"
                }
            },
            "required": ["fasta"]
        },
        "variant_set_options": {
            "title": "Variant set options",
            "type": "object",
            "fa_icon": "fas fa-dna",
            "description": "Options for providing an existing known variant set or generating one via bootstrapping.",
            "properties": {
                "known_variants_vcf": {
                    "type": ["string", "null"],
                    "fa_icon": "fas fa-copy",
                    "format": "file-path",
                    "pattern": "^\\S+\\.vcf\\.gz$",
                    "exists": true,
                    "mimetype": "text/plain",
                    "description": "Path to known variant set vcf file.",
                    "help_text": "If none provided, it will be generated via bootstrap algorithm."
                },
                "known_variants_tbi": {
                    "type": ["string", "null"],
                    "fa_icon": "fas fa-copy",
                    "format": "file-path",
                    "pattern": "^\\S+\\.vcf\\.gz\\.tbi$",
                    "exists": true,
                    "mimetype": "text/plain",
                    "description": "Path to known variant set tabix index tbi file.",
                    "help_text": "If none provided, it will be generated automatically from the known index file, if provided."
                },
                "bootstrapping_rounds": {
                    "type": ["integer", "null"],
                    "minimum": 1,
                    "maximum": 3,
                    "fa_icon": "fas fa-arrows-spin",
                    "description": "Number of bootstrapping rounds to perform when generating a variant set.",
                    "help_text": "Cannot be set if known_variants_vcf is provided."
                },
                "include_scaffolds": {
                    "type": ["string", "array", "null"],
                    "items": { "type": "string" },
                    "fa_icon": "fas fa-check",
                    "description": "List of scaffolds (e.g., chromosome or contig names) to include using vcftools. Can be passed as single string, array of strings, path to text file (including .bed). Leave empty to choose all by default.",
                    "help_text": "Example: [\"chrM\", \"MT\", \"chrUn_Mito_1\"]"
                },
                "exclude_scaffolds": {
                    "type": ["string", "array", "null"],
                    "items": { "type": "string" },
                    "fa_icon": "fas fa-ban",
                    "description": "List of scaffolds (e.g., chromosome or contig names) to exclude using vcftools. Can be passed as single string, array of strings, path to text file (including .bed). Leave empty to disable exclusion.",
                    "help_text": "Example: [\"chrM\", \"MT\", \"chrUn_Mito_1\"]"
                }
            },
            "allOf": [
                {
                    "if": {
                        "properties": {
                            "known_variants_vcf": { "type": "string" }
                        },
                        "required": ["known_variants_vcf"]
                    },
                    "then": {
                        "properties": {
                            "bootstrapping_rounds": { "type": "null" }
                        }
                    }
                },
                {
                    "if": {
                        "properties": {
                            "bootstrapping_rounds": { "type": "integer" }
                        },
                        "required": ["bootstrapping_rounds"]
                    },
                    "then": {
                        "properties": {
                            "known_variants_vcf": { "type": "null" },
                            "known_variants_tbi": { "type": "null" }
                        },
                        "required": ["bootstrapping_rounds"]
                    }
                },
                {
                    "if": {
                        "properties": {
                            "include_scaffolds": {
                                "not": { "type": "null" }
                            }
                        },
                        "required": ["include_scaffolds"]
                    },
                    "then": {
                        "properties": {
                            "exclude_scaffolds": {
                                "type": "null"
                            }
                        }
                    }
                },
                {
                    "if": {
                        "properties": {
                            "exclude_scaffolds": {
                                "not": { "type": "null" }
                            }
                        },
                        "required": ["exclude_scaffolds"]
                    },
                    "then": {
                        "properties": {
                            "include_scaffolds": {
                                "type": "null"
                            }
                        }
                    }
                }
            ]
        },
        "skip_stages_options": {
            "title": "Stages options",
            "type": "object",
            "fa_icon": "fas fa-forward",
            "description": "Options for pipeline stages to run.",
            "properties": {
                "hard_filter_variants": {
                    "type": "boolean",
                    "description": "Whether to perform hard filtering of variants.",
                    "fa_icon": "fas fa-filter",
                    "help_text": "Enable this option to perform hard filtering of variants using GATK VariantFiltration after variant calling. This step applies a set of predefined filters to the called variants based on quality metrics to remove low-quality variant calls."
                },
                "skip_bqsr": {
                    "type": "boolean",
                    "description": "Whether to skip Base Quality Score Recalibration (BQSR) on the aligned reads.",
                    "fa_icon": "fas fa-forward",
                    "help_text": "Disable this option to perform BQSR using GATK. This step can improve variant calling accuracy by correcting systematic errors made by the sequencer when estimating the quality scores of each base call. This stage runs only if a known variant set is provided via --known_variants_vcf."
                },
                "skip_variant_calling": {
                    "type": "boolean",
                    "description": "Whether to skip the variant calling stage to identify variants from the aligned reads.",
                    "fa_icon": "fas fa-forward",
                    "help_text": "Disable this option to perform variant calling using GATK HaplotypeCaller and BCFtools mpileup. This stage generates a VCF file containing the identified variants."
                },
                "skip_intersection_thinning": {
                    "type": "boolean",
                    "description": "Whether to skip the intersection and thinning of the called variants to a set of high-quality, independent variants for relatedness estimation.",
                    "fa_icon": "fas fa-forward",
                    "help_text": "Disable this option to perform intersection and thinning of the called variants using GATK SelectVariants and VCFtools. This step reduces the variant set to a subset of high-quality, independent variants for downstream relatedness estimation."
                },
                "skip_relatedness_estimation": {
                    "type": "boolean",
                    "description": "Whether to skip the relatedness estimation stage.",
                    "fa_icon": "fas fa-forward",
                    "help_text": "Disables this option to estimate relatedness between samples using the called variants. This stage uses tools like READ, BREADR and NGSrelate to compute relatedness metrics."
                }
            },
            "allOf": [
                {
                    "if": {
                        "properties": {
                            "skip_variant_calling": { "const": true }
                        },
                        "required": ["skip_variant_calling"]
                    },
                    "then": {
                        "properties": {
                            "skip_intersection_thinning": { "const": true },
                            "skip_relatedness_estimation": { "const": true }
                        },
                        "required": ["skip_intersection_thinning", "skip_relatedness_estimation"]
                    }
                },
                {
                    "if": {
                        "properties": {
                            "skip_intersection_thinning": { "const": true }
                        },
                        "required": ["skip_intersection_thinning"]
                    },
                    "then": {
                        "properties": {
                            "skip_relatedness_estimation": { "const": true }
                        },
                        "required": ["skip_relatedness_estimation"]
                    }
                }
            ]
        },
        "institutional_config_options": {
            "title": "Institutional config options",
            "type": "object",
            "fa_icon": "fas fa-university",
            "description": "Parameters used to describe centralised config profiles. These should not be edited.",
            "help_text": "The centralised nf-core configuration profiles use a handful of pipeline parameters to describe themselves. This information is then printed to the Nextflow log when you run a pipeline. You should not need to change these values when you run a pipeline.",
            "properties": {
                "custom_config_version": {
                    "type": "string",
                    "description": "Git commit id for Institutional configs.",
                    "default": "master",
                    "hidden": true,
                    "fa_icon": "fas fa-users-cog"
                },
                "custom_config_base": {
                    "type": "string",
                    "description": "Base directory for Institutional configs.",
                    "default": "https://raw.githubusercontent.com/nf-core/configs/master",
                    "hidden": true,
                    "help_text": "If you're running offline, Nextflow will not be able to fetch the institutional config files from the internet. If you don't need them, then this is not a problem. If you do need them, you should download the files from the repo and tell Nextflow where to find them with this parameter.",
                    "fa_icon": "fas fa-users-cog"
                },
                "config_profile_name": {
                    "type": "string",
                    "description": "Institutional config name.",
                    "hidden": true,
                    "fa_icon": "fas fa-users-cog"
                },
                "config_profile_description": {
                    "type": "string",
                    "description": "Institutional config description.",
                    "hidden": true,
                    "fa_icon": "fas fa-users-cog"
                },
                "config_profile_contact": {
                    "type": "string",
                    "description": "Institutional config contact information.",
                    "hidden": true,
                    "fa_icon": "fas fa-users-cog"
                },
                "config_profile_url": {
                    "type": "string",
                    "description": "Institutional config URL link.",
                    "hidden": true,
                    "fa_icon": "fas fa-users-cog"
                }
            }
        },
        "generic_options": {
            "title": "Generic options",
            "type": "object",
            "fa_icon": "fas fa-file-import",
            "description": "Less common options for the pipeline, typically set in a config file.",
            "help_text": "These options are common to all nf-core pipelines and allow you to customise some of the core preferences for how the pipeline runs.\n\nTypically these options would be set in a Nextflow config file loaded for all pipeline runs, such as `~/.nextflow/config`.",
            "properties": {
                "version": {
                    "type": "boolean",
                    "description": "Display version and exit.",
                    "fa_icon": "fas fa-question-circle",
                    "hidden": true
                },
                "publish_dir_mode": {
                    "type": "string",
                    "default": "copy",
                    "description": "Method used to save pipeline results to output directory.",
                    "help_text": "The Nextflow `publishDir` option specifies which intermediate files should be saved to the output directory. This option tells the pipeline what method should be used to move these files. See [Nextflow docs](https://www.nextflow.io/docs/latest/process.html#publishdir) for details.",
                    "fa_icon": "fas fa-copy",
                    "enum": ["symlink", "rellink", "link", "copy", "copyNoFollow", "move"],
                    "hidden": true
                },
                "email_on_fail": {
                    "type": "string",
                    "description": "Email address for completion summary, only when pipeline fails.",
                    "fa_icon": "fas fa-exclamation-triangle",
                    "pattern": "^([a-zA-Z0-9_\\-\\.]+)@([a-zA-Z0-9_\\-\\.]+)\\.([a-zA-Z]{2,5})$",
                    "help_text": "An email address to send a summary email to when the pipeline is completed - ONLY sent if the pipeline does not exit successfully.",
                    "hidden": true
                },
                "plaintext_email": {
                    "type": "boolean",
                    "description": "Send plain-text email instead of HTML.",
                    "fa_icon": "fas fa-remove-format",
                    "hidden": true
                },
                "max_multiqc_email_size": {
                    "type": "string",
                    "description": "File size limit when attaching MultiQC reports to summary emails.",
                    "pattern": "^\\d+(\\.\\d+)?\\.?\\s*(K|M|G|T)?B$",
                    "default": "25.MB",
                    "fa_icon": "fas fa-file-upload",
                    "hidden": true
                },
                "monochrome_logs": {
                    "type": "boolean",
                    "description": "Do not use coloured log outputs.",
                    "fa_icon": "fas fa-palette",
                    "hidden": true
                },
                "multiqc_config": {
                    "type": "string",
                    "format": "file-path",
                    "description": "Custom config file to supply to MultiQC.",
                    "fa_icon": "fas fa-cog",
                    "hidden": true
                },
                "multiqc_logo": {
                    "type": "string",
                    "description": "Custom logo file to supply to MultiQC. File name must also be set in the MultiQC config file",
                    "fa_icon": "fas fa-image",
                    "hidden": true
                },
                "multiqc_methods_description": {
                    "type": "string",
                    "description": "Custom MultiQC yaml file containing HTML including a methods description.",
                    "fa_icon": "fas fa-cog"
                },
                "validate_params": {
                    "type": "boolean",
                    "description": "Boolean whether to validate parameters against the schema at runtime",
                    "default": true,
                    "fa_icon": "fas fa-check-square",
                    "hidden": true
                },
                "pipelines_testdata_base_path": {
                    "type": "string",
                    "fa_icon": "far fa-check-circle",
                    "description": "Base URL or local path to location of pipeline test dataset files",
                    "default": "https://raw.githubusercontent.com/nf-core/test-datasets/",
                    "hidden": true
                },
                "pipelines_testdata_genomicrelatedness_path_suffix": {
                    "type": "string",
                    "fa_icon": "far fa-check-circle",
                    "description": "URL suffix for nf-core/genomicrelatedness's test dataset repository",
                    "default": "refs/heads/genomicrelatedness/",
                    "hidden": true
                },
                "pipelines_testdata_rnaseq_path_suffix": {
                    "type": "string",
                    "fa_icon": "far fa-check-circle",
                    "description": "URL suffix for nf-core/rnaseq's test dataset repository",
                    "default": "refs/heads/rnaseq/",
                    "hidden": true
                },
                "trace_report_suffix": {
                    "type": "string",
                    "fa_icon": "far calendar",
                    "description": "Suffix to add to the trace report filename. Default is the date and time in the format yyyy-MM-dd_HH-mm-ss.",
                    "hidden": true
                },
                "help": {
                    "type": ["boolean", "string"],
                    "description": "Display the help message."
                },
                "help_full": {
                    "type": "boolean",
                    "description": "Display the full detailed help message."
                },
                "show_hidden": {
                    "type": "boolean",
                    "description": "Display hidden parameters in the help message (only works when --help or --help_full are provided)."
                }
            }
        }
    },
    "allOf": [
        {
            "$ref": "#/$defs/input_output_options"
        },
        {
            "$ref": "#/$defs/reference_genome_options"
        },
        {
            "$ref": "#/$defs/variant_set_options"
        },
        {
            "$ref": "#/$defs/skip_stages_options"
        },
        {
            "$ref": "#/$defs/institutional_config_options"
        },
        {
            "$ref": "#/$defs/generic_options"
        }
    ]
}
